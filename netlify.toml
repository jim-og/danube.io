[build]
    command = "npm install --legacy-peer-deps && npm run build"
    publish="build"
    command="npm run build"
[build.environment]
    NODE_VERSION = "v20"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200